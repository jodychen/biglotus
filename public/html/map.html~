
<!-- might move to font awesome icons if we need another icon from that library. the ajax loader is only 4kb.
<section id="test" class="fa fa-spinner fa-spin"></section>
-->
<section id="loading">
    <h2>Map is Loading ...</h2>
    <img src="img/open-source/ajax-loader.gif" />
</section>

<ui-gmap-google-map center='map.center' zoom='map.zoom'>
  <ui-gmap-marker idKey='marker1.id' coords='marker1.coords' options='marker1.options'></ui-gmap-marker>
  <ui-gmap-marker idKey='marker2.id' coords='marker2.coords' options='marker2.options'></ui-gmap-marker>
</ui-gmap-google-map>

  <!--    <div ng-show="position">        
        <<p>{{ position | json }}</p>
        <p>{{ position.coords.latitude }}</p> 
        <p>{{ position.coords.longitude }}</p>
      </div>
      <div ng-hide="position">
        <p>{{ message }}</p>
      </div> -->
 


<!-- <div ng-show="zipCode">        
      <p>{{ zipCode }}</p>
      </div>
      <div ng-hide="zipCode">
        <p>{{ message }}</p>
      </div>
    </div>
 -->

